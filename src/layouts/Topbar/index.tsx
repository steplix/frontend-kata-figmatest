import React from 'react';
import { Box } from '@mui/material';

const Topbar = () => {
  return <Box sx={{ width: '100%', backgroundColor: 'red', height: '3.5rem' }}>Hola</Box>;
};

export default Topbar;
